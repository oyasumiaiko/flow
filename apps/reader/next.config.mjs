/** @type {import('next').NextConfig} */
const config = {
  pageExtensions: ['ts', 'tsx'],
  i18n: {
    locales: ['en-US', 'zh-CN', 'ja-JP'],
    defaultLocale: 'en-US',
  },
}

export default config
